# ChartOps Backend

Elysia + Bun runtime.

## Commands
```bash
bun dev     # dev server with watch mode
bun run dev # same as above
```

## Notes
- Entry point: `src/index.ts`
- Port: 8080
- No tests configured
- No lint/typecheck configured
- `bun-types` for type support
